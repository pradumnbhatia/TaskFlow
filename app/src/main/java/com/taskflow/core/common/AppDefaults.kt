package com.taskflow.core.common

/**
 * v1 has no accounts (see docs/PRODUCT_SPEC.md, "explicitly out of scope"), so the
 * display name shown on Home/Profile is a fixed placeholder rather than user profile data.
 */
const val DEFAULT_USER_NAME = "Pradumn"
